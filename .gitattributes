# Auto-detect text files and normalize to LF
* text=auto

# Always use LF for JavaScript, JSON, Markdown, TypeScript, etc.
*.js     text eol=lf
*.ts     text eol=lf
*.json   text eol=lf
*.md     text eol=lf
*.yml    text eol=lf
*.yaml   text eol=lf
*.lock   text eol=lf

# Optional: treat shell scripts and config files as LF-only
*.sh     text eol=lf
*.env    text eol=lf

# Ignore OS-specific metadata
.DS_Store binary
Thumbs.db binary